// Generated by LiveScript 1.3.1
(function(){
  var _, moment, fs, $, __, debug, _module;
  _ = require('lodash');
  moment = require('moment');
  fs = require('fs');
  $ = require('bluebird');
  __ = require('shelljs');
  debug = require('debug')('grader');
  _module = function(){
    var grade, iface;
    grade = function(payload){
      var graderApi;
      if (payload.lang != null) {
        graderApi = require("./lang/" + payload.lang);
        return graderApi.grade(payload);
      } else {
        throw "Sorry, you need to specify a valid grader.";
      }
    };
    iface = {
      grade: grade
    };
    return iface;
  };
  module.exports = _module();
}).call(this);
