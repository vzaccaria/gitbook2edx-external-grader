// Generated by LiveScript 1.3.1
(function(){
  "use strict";
  var debug, _module;
  debug = require('debug')('grader');
  _module = function(){
    var grade, iface;
    debug("Started");
    grade = function*(response, payload){
      var graderApi;
      debug("Response: ");
      debug(response);
      debug("Payload");
      debug(payload);
      if (payload.lang != null) {
        graderApi = require("./lang/" + payload.lang);
        return graderApi.grade(response, payload);
      } else {
        throw "Sorry, you must specify a language";
      }
    };
    iface = {
      grade: grade
    };
    return iface;
  };
  module.exports = _module();
}).call(this);
